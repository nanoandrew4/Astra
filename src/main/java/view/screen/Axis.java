package view.screen;

/**
 * In RotateAnimData, used when animating rotations using RotateTransition in Screen, defines on which axis
 * the rotation animation is performed.
 */
public enum Axis {
	X, Y, Z
}
